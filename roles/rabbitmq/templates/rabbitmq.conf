listeners.tcp.1 = 127.0.0.1:5672
{% for ip in ansible_all_ipv4_addresses | ipaddr('private') | ipaddr('address') %}
listeners.tcp.{{ loop.index + 1}} = {{ ip }}:5672
{% endfor %}
vm_memory_high_watermark.absolute = {{ rabbitmq_memlimit }}

